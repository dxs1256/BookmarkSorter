name: Build & Release Chrome Extension

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Get next version
      id: version
      run: |
        # 读取上次版本号 +1
        LAST_VERSION=$(git tag --list 'v*' --sort=-version:refname | head -n1 | sed 's/v//')
        if [[ -z "$LAST_VERSION" ]]; then
          NEXT_VERSION="1.0.0"
        else
          # 语义化版本递增
          IFS='.' read -r MAJOR MINOR PATCH <<< "$LAST_VERSION"
          NEXT_VERSION="${MAJOR}.${MINOR}.$((PATCH + 1))"
        fi
        echo "version=v$NEXT_VERSION" >> $GITHUB_OUTPUT
        echo "Next version: v$NEXT_VERSION"
        
    - name: Create ZIP
      run: |
        zip -r "bookmark-sorter-${{ steps.version.outputs.version }}.zip" . \
        -x "*.git*" ".github/*" "README.md" ".gitignore" "builds/*"
        
    - name: Create Release & Upload
      uses: softprops/action-gh-release@v2
      with:
        tag_name: ${{ steps.version.outputs.version }}
        name: "🔥 Bookmark Sorter ${{ steps.version.outputs.version }}"
        body: |

          ## ✨ 核心功能
          - ✅ **递归排序**所有子文件夹  
          - ✅ **视觉长度排序**（汉字×2，英文×1，短的在前）  
          - ✅ **书签在前**，文件夹在后  
          - ✅ **双触发方式**：右键菜单 + 图标点击  

          ## 📦 安装步骤
          1. 下载 → 解压  
          2. `chrome://extensions/` → 开发者模式  
          3. **加载已解压的扩展程序** → 选择文件夹  
          4. 任意页面**右键 → 全部书签递归排序**

          ---
          **⭐ 喜欢请点 Star 支持！**
        draft: false
        prerelease: false
        files: bookmark-sorter-${{ steps.version.outputs.version }}.zip
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
