name: Build & Release Chrome Extension

on:
  workflow_dispatch:  # ✅ 手动触发

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Generate version
      id: version
      run: |
        echo "version=$(date +%Y%m%d)-${GITHUB_SHA::7}" >> $GITHUB_OUTPUT
        
    - name: Create ZIP
      run: |
        zip -r "bookmark-sorter-v${{ steps.version.outputs.version }}.zip" . \
        -x "*.git*" ".github/*" "README.md" ".gitignore" "builds/*"
        
    - name: Create Release & Upload
      # ❌ 临时移除 if 条件，让手动触发也能发布
      uses: softprops/action-gh-release@v2
      with:
        tag_name: v${{ steps.version.outputs.version }}
        name: "🔥 Bookmark Sorter v${{ steps.version.outputs.version }}"
        body: |
          ## 🔥 最新版 v${{ steps.version.outputs.version }}
          
          ✅ 递归排序所有子文件夹
          ✅ 视觉长度排序（短的在前）
          ✅ 书签在前，文件夹在后
          ✅ 右键菜单 + 图标点击
          
          ### 📥 下载 👇
        draft: false
        prerelease: false
        files: bookmark-sorter-v${{ steps.version.outputs.version }}.zip
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
